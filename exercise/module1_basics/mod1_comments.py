# Comments are introduced with hash signs like this.
# ctrl + / 
# Everything starting at the hash sign until the end of the line is ignored by Python.
print('sample') # here is a mid-line comment
# here is an all-line comment

# we may want to document our code if someone else is going to read it
# if we are going to go back to our code after some time


# note that the variables are strings, not number!
var1 = '23'
var2 = '54'

# If we intend to use the code in the future, it's better to comment it out rather than delete it.